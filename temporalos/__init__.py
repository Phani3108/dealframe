"""DealFrame — Video → Structured Negotiation Intelligence Engine.

The Python package is still published under the legacy ``temporalos`` name
for backwards-compatibility, but the product is now DealFrame.
"""

__version__ = "0.1.0"
